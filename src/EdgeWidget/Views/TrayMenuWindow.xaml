<Window x:Class="EdgeWidget.Views.TrayMenuWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:EdgeWidget.Ui"
        Title="EdgeWidget"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        UseLayoutRounding="True"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Grayscale">

    <!-- 20 px margin leaves room for the shadow (TrayMenuWindow.ShadowMargin). -->
    <Grid Margin="20">
        <Border CornerRadius="14" Background="#0A0A0A">
            <Border.Effect>
                <DropShadowEffect BlurRadius="28" ShadowDepth="8" Direction="270" Color="Black" Opacity="0.5" />
            </Border.Effect>
        </Border>

        <Border x:Name="MenuBody" CornerRadius="14" Background="#0A0A0A" BorderBrush="#232325" BorderThickness="1" Padding="6">
            <StackPanel MinWidth="196">
                <TextBlock Text="EdgeWidget" FontSize="12" Foreground="#8E8E93" Margin="12,6,12,6" />

                <Button x:Name="RefreshButton" Style="{StaticResource MenuButton}" Click="OnRefreshClick"
                        AutomationProperties.AutomationId="RefreshButton" AutomationProperties.Name="Actualizar ahora">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{x:Static ui:Icons.RefreshImage}" Width="16" Height="16" VerticalAlignment="Center" />
                        <TextBlock Text="Actualizar ahora" Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>

                <Border Height="1" Background="#1F1F21" Margin="10,4" />

                <Button x:Name="ExitButton" Style="{StaticResource MenuButton}" Click="OnExitClick"
                        AutomationProperties.AutomationId="ExitButton" AutomationProperties.Name="Salir">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{x:Static ui:Icons.PowerImage}" Width="16" Height="16" VerticalAlignment="Center" />
                        <TextBlock Text="Salir" Margin="10,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
